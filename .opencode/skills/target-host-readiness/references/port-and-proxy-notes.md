# Port and Proxy Notes

## 端口检查不应只看一个数字

至少考虑：

-服务自身监听端口
-反向代理前端端口（80/443）
-现有服务占用
-同机多个实例的端口规划
-防火墙与安全组

## 常见误区

- “8080没被占用” 不等于外部用户就能访问8080
-服务只监听 `127.0.0.1` 时，可能必须通过反向代理暴露
-证书终止点在nginx/Caddy/LB，而不是应用自身
