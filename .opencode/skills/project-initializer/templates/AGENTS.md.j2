# Project Agent Guide

## Project Goal
{{ project_goal }}

## Project Type
{{ project_type }}

## Working Principles
- Understand first, then act.
- Plan before large changes.
- Do not overwrite existing files without confirmation.
- Do not delete files without explicit instruction.
- Keep tasks, sources, and outputs separated.
- Do not operate on other repositories (even with access); raise issues instead.
- When network operations fail (SSH, package install, git clone, API calls), retry at least twice before changing approach — transient failures are common and do not warrant strategy changes.
- Do not track installed skill/command/agent files in version control — they are derived from the skill source (packs) and regenerated by the installer. Add platform skill directories to `.gitignore`.
- Do not create todo items with external blockers (e.g. "after user approval", "waiting for feedback"). The todo system tracks agent-completable work only. When your part is done, mark it completed and state the blocker in your response text. Create follow-up todos only after the blocker is resolved. This prevents infinite continuation loops from automated todo hooks.

## Directory Map
{{ directory_map }}

## Skills
{{ skills }}

## Quality Gates
{{ quality_gates }}

## Do Not
- Do not write secrets.
- Do not hide shell commands.
- Do not perform unauthorized offensive security operations.
